# Frontend Environment Variables

# Backend API URL (leave empty for local development with proxy)
NEXT_PUBLIC_API_URL=

# Backend API URL for server-side proxy rewrites (defaults to localhost:8000)
API_URL=http://localhost:8000

# Demo mode - uses mock data, no backend required (default: false)
# Set to "true" for Vercel demo deployment
DEMO_MODE=false
